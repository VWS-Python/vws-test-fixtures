version: 2

# We do this because at the time of writing we need "image: latest" for Python
# 3.9.
build:
  os: ubuntu-20.04
  tools:
    python: "3.9"

python:
  install:
     - method: pip
       path: .
       extra_requirements:
           - dev

sphinx:
  builder: html
  fail_on_warning: true
